# ANGELA v10 — Production API Route Contract

The route contract now covers the full core application flow:
Auth → Profile → Tasks → Daily Challenges → Referral → Wallet/Vesting.

Important:
- Route definitions are a contract, not a claim that external platform verification is already connected.
- X/YouTube/Telegram activity verification depends on the permissions and APIs available to the deployed application.
- Points and vesting must always be computed server-side.
